import LoginForm from "../components/LoginForm/LoginForm";

const Home = () => {
	return (
		<div className="w-screen flex justify-center items-center h-screen bg-white">
			<LoginForm />
		</div>
	);
};

export default Home;
